<%- content_for :nav do %>
   <p>text for hello </p>
   <p>just saying some hi</p>
<%- end%>
<%- content_for :nav do %>
   <p>text for hello </p>
   <p>just saying some hi again</p>
<%- end%>

<!-- to render a partial -->

<%=render "search"%>


<h1>Listing issues</h1>

<table>
  <thead>
    <tr>
      <th>Title <%= link_to "sort", issues_path(sort: "title")%></th>
      <th>Description <%= link_to "sort", issues_path(sort: "description")%></th>
      <th>No follers <%= link_to "sort", issues_path(sort: "no_follers")%></th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @issues.each do |issue| %>
      <tr>
        <td><%= issue.title %></td>
        <td><%= issue.description %></td>
        <td><%= issue.no_follers %></td>
        <td><%= link_to 'Show', issue %></td>
        <td><%= link_to 'Edit', edit_issue_path(issue) %></td>
        <td><%= link_to 'Destroy', issue, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>


  
</table>

<br>

<%= link_to 'New Issue', new_issue_path %>
